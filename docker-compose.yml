version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - NODE_ENV=production
    # Uncomment and add your environment variables here
    # env_file:
    #   - .env.production
    restart: unless-stopped
